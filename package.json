{
  "name": "rent-house-review-monorepo",
  "version": "1.0.0",
  "description": "Cross-platform rent house review application",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/backend",
    "packages/types"
  ],
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --recursive build",
    "test": "pnpm run --recursive test",
    "lint": "pnpm run --recursive lint",
    "format": "pnpm run --recursive format"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^30.1.1",
    "@types/jest": "^30.0.0"
  },
  "packageManager": "pnpm@10.15.0"
}